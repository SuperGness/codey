# Codey 内部开发文档

本文面向维护者，概括现有功能的实现方式和必要边界。用户功能介绍见 README.md，变更历史由 Git 保存。

## 整体结构

- Rust 主程序负责 Codex Electron 的启动、进程管理、配置、路由和本地数据维护。
- React 控制台构建后嵌入程序，经 CDP 挂载到 Codex 页面；完整界面按需加载，轻量脚本负责日常页面增强。
- `src/` 保存前端，`public/` 保存注入脚本，`backend/src/` 保存后端，`backend/resources/` 保存运行规则。
- `vendor/CodeyRuntime/` 提供应用发现、CDP、配置事务、会话存储和平台能力；FastCtx 作为配套程序分发。

## 启动与退出

启动先恢复 Codey 临时状态、检查更新并读取配置；受控 Codex 停止后完成会话维护、模型目录准备和诊断保护，再建立路由、Hook 与运行配置，启动客户端并安装页面桥接。

- 主进程优先使用 `NODE_OPTIONS=--require` 加载运行配置，Inspector evaluate 为回退；二者不同时使用。启动前检测 Electron fuse；NODE_OPTIONS 不可用且 Inspector 仍可用时改走 Inspector，否则使用包装器，必要配置无法确认时停止启动。控制台运行状态展示已确认的注入路径。
- `--require` 启动环境关闭 Sparkle 运行期更新路径，更新仍由 Codey 的启动前检查和用户操作处理；该变量只传给桌面主进程，不传给 app-server 包装器。
- 包装器调用应用内置 CLI，并检查配套工具宿主；通过认证握手或运行记录确认目标已执行。该确认不代表 app-server 已完成初始化。
- 辅助进程过滤环境变量时，包装器从已保存应用位置恢复 CLI。独立工具调用原样转发；受控 app-server 必须携带本次配置，禁止误入桌面启动及进程清理流程。
- Windows Store 按当前用户注册包定位，每次尝试按原 package family 刷新路径，激活后核验实际身份。包切换时先成功清理再重试，不跨正式版、Beta 或发布者切换。
- Windows 最多尝试两次，仅暂时性启动错误或已确认包切换允许重试。权限、配置、身份和清理失败直接报错；进程查询失败不能视为已退出或已清理。
- Store 配套文件暂存于 Codey 私有目录，通过清单、大小及复制时哈希校验复用。诊断记录阶段、进程身份和退出码，不记录令牌或完整配置。
- 宠物精简只调整对应开关，保留其他 JSON 值；主文件失败时尝试备份，仍失败则记录原因并继续启动。精简开启时，主进程跳过隐藏 avatar overlay 启动预热，并对隐藏浮窗恢复 `backgroundThrottling`；语音仍可按需创建浮窗。
- Windows 主进程拦截 Codex 周期进程采样 Worker（已知文件名、线程名，或同时具备 CIM/WMI、`Win32_Process`、`Win32_Perf*Process`、PowerShell 与 worker 消息的源码特征）。未知 Worker 只对不超过 2 MB 的普通文件检查源码，缓存随文件变化失效。一次性 `Win32_ComputerSystem` 厂商查询不在范围内；不经 renderer CDP 安装 WMI 探针。
- 可兼容设置热更新，启动参数、模型能力及上下文预算等变化需要重启。前端等待超时只停止查询，不取消后台重启，可重新查询状态。

退出时先确认 Codex 停止，再关闭监视任务、回收进程、恢复临时配置，最后停止路由。停止或恢复失败时保留已建立运行时供重试；启动尚未完成就无法终止进程的情况可能需要人工退出残留 Codex。

## 配置与数据安全

- Codey 配置单独保存于系统配置目录，保留三份有效滚动备份；`CODEX_HOME` 非空时优先使用。
- Codex 配置和 `auth.json` 原则上只读；仅维护明确属于 Codey 的路由恢复桩、临时 Hook、角色副本及遗留项。持久数据修改使用所有权校验、快照复核、备份和原子替换。
- 官方账号通过原生登录探针及严格端点、认证检查识别，不依据模糊文本或第三方配置中的残留凭据判定已登录。
- Unix 私有配置、备份和日志限制当前用户访问；会话传输文件使用 0600、目录使用 0700。第三方密钥和通知凭据仍以明文保存在私有配置及备份中，部分字段会返回控制台供编辑。
- 普通错误日志只保存脱敏失败信息，不保存提示词、正常响应或认证值。请求日志的上游错误正文有单独边界，见后文。

## 线路与模型

本地路由使用不可变线路快照，每个请求固定自己的配置。官方账号与第三方凭据隔离，转发前还原模型标识并移除 Codey 专用元数据。

线路请求头在保存和发送前共用校验，名称不区分大小写，重复名称、非法值和超限配置会被拒绝。普通头先合并默认值与允许转发的客户端值，再应用线路覆盖；`null` 或空字符串表示删除，删除标记不会以空值头发送，认证和协议必需头保留专门约束。逐跳头及 `Connection` 声明的请求头不转发；Codey 内部标识只写入本地日志和下游响应，不随上游请求外发，自动生成的提示缓存键以普通 UUID 形态呈现。模型列表使用同一规则，已保存覆盖优先于来源 Provider 的请求头，并禁止自动重定向。

- 选择器以带线路的稳定模型 ID 区分同名模型；已解析的页面 `turn/start` 使用上游原始模型名，并单独传递线路信息。
- 模型别名历史不含凭据，支持线路变更后恢复旧任务；原始模型名可包含 `/`，历史别名仅还原一次。归属不明或存在歧义时拒绝请求，不猜测默认模型。
- 路由开启时，本机 app-server 使用本次 CLI 配置；新建、恢复和分支请求统一指定 `codey_router`，清除可覆盖供应商和端点的请求配置。主进程发送入口或 CLI 输入转发执行同样检查。
- 路由关闭时沿用原生 Provider 和可用模型，历史任务在恢复入口转换；不批量改写 rollout 或 SQLite 中的历史 Provider。
- 模型目录优先复用可信本机元数据，第三方能力不从无关模型继承。第三方线路的 Ultra 选项由模型目录和用户推理设置共同决定。空目录不传给 Codex；回退内置目录时保留用户目录，清理 Codey 旧引用。
- 模型增删可更新路由及页面列表，能力或预算变化仍按实际运行配置提示重启；热更新先校验兼容性，再替换运行快照。待重启时保留最近已生效的模型目录，能力校验仍以启动配置为准。
- Images API 只向 OpenAI 兼容线路透明转发。网页搜索、自动审核和远程压缩须满足配置与模型能力条件。
- Fast 选项通过页面原生控件兼容显示和保存，覆盖直连及路由模式；选项可见不代表上游承诺加速。
- 主进程标题补丁可用时优先使用官方账号轻量模型，否则使用默认第三方线路的同名模型或默认模型；失败保留临时标题。CLI 包装入口沿用原生行为。

## 协议转发与连接恢复

原生 Responses 尽量保留原始请求和响应，HTTP 转发把上游端到端响应头（含 `x-codex-turn-state` 粘性路由令牌）原样透传给下游，仅重写传输层头；Chat Completions、Anthropic Messages 转换消息、工具、图片、用量及流式事件。协议无法表达的必要内容在发送前拒绝，不静默裁剪历史。

- HTTP 入口使用 HTTP 上游；WebSocket 入口按线路能力、系统代理和退避状态选择传输，连接按线路、配置和认证身份隔离复用。WSS 共享 TLS 配置与按服务端域名隔离的会话缓存，保留证书校验并禁用 0-RTT；服务端不接受会话恢复时执行完整握手。
- 只有握手或发送前失败可以回到同一线路的 HTTP 流式请求；请求一旦尝试发送，不自动重放、不跨供应商切换。
- 未知 WebSocket 能力同一身份只允许一个探测；不支持结果缓存一小时，其他失败按 5、15、30、60 秒退避。配置变化仅清理受影响线路并回收过时空闲连接。
- 原生 WebSocket 保存完整成功历史；下一轮断线续接时，先校验任务、身份、响应引用及工具配对，再展开完整输入。无法恢复时明确报错，已发送的失败请求不参与重放。
- 近期历史仅存于路由进程内：共享缓存最多 64 个快照、16 MiB 估算大小、保留 5 分钟，按任务和身份隔离并计入共享预算；不跨进程或机器恢复，也不保证保留所有分支。
- Chat 思考内容按标准 Responses 字段转递，下一轮还原 `reasoning_content`；缺失内容不补造。工具调用、结果和终态继续校验顺序及完整性。
- SSE 增量分帧并校验真实终态，提前断开不能伪装成功，每个请求最多尝试一个结束事件。已开始的 HTTP SSE 定期发送注释心跳，写入失败后停止上游等待。
- WebSocket 关闭可取消当前等待，后续应用消息有界排队；队列满时控制帧可能延迟。HTTP 合法半关闭不视为取消，响应头前或非流式等待仍依赖写入错误及超时。
- 请求、解压输出、WebSocket 单消息及累计响应上限为 64 MiB；请求和历史分别受 256 MiB 估算预算约束。后台转换持有配额直至结束，预算不等于实际内存上限或 Token 窗口。
- 普通响应体读取有固定 15 分钟期限及空闲超时，压缩另设总期限。上游 HTTP 错误保留状态码及脱敏信息，上下文超限保留可识别的结构化语义。

## 上下文与远程压缩

历史、Token 记账、压缩触发和结果提交由 Codex 管理；Codey 配置模型目录并检查压缩请求，不维护第二份可写会话历史。

- 第三方模型可声明窗口、自动压缩阈值和输出预留。预算按显式配置、旧 1M 声明、精确官方元数据、未知模型保守值的顺序解析；未知模型使用 32768 Token、95% 有效比例。
- 输出预留保守投影到有效窗口，自动阈值不超过窗口的 90% 和扣除预留后的容量；这不限制实际输出长度，也不能扩大服务端容量。
- 预算变化要求重启；目录无法应用时拒绝保存并恢复快照。已有预算导致启动失败时，清理成功后可由用户选择恢复预算并重试。
- 关闭路由时保留预算但禁止编辑；官方模型入口不展示预算和 1M 设置，保存官方模型时保留已有相关配置。
- 旧 `/responses/compact` 和 V2 `compaction_trigger` 分别识别，并检查线路原生能力。同会话重叠压缩返回冲突，无会话标识时按输入摘要进行进程内在途去重；WebSocket 压缩转为 HTTP 前须恢复完整续接历史，无法恢复则拒绝发送。
- 压缩请求从等待响应头到读完响应共用 120 秒总期限。V2 完成事件省略输出时，从连续、无冲突的已完成输出项恢复候选；完整加密项使用响应大小上限，再检查完成状态及唯一非空压缩项。上下文超限返回 400；失败或取消释放锁，不自动重放。
- Chat/Anthropic 不接收原生压缩项；不可移植引用须先完整展开，否则拒绝请求。不会自造加密格式或删除压缩项后继续生成。
- 压缩期间固定线路快照。跨原生供应商的加密结果来源验证、独立摘要模型、语义质量、持久版本原子提交及二次摘要仍依赖宿主能力；当前检查不保证压缩后必然满足 Token 窗口。

## 控制台与页面增强

- CDP 负责桥接、注入及健康检查；仅确认桥接缺失时重建，页面忙或超时保持保守状态。用户脚本在同一文档成功执行后不因恢复重复运行；会话工具脚本重载时先释放上一次安装的监听与定时器。共享 DOM 观察器按订阅者各自的选项分发变更记录。
- 前端使用 HeroUI、React Aria 和 Tailwind；内嵌界面通过 Shadow DOM 隔离样式，统一管理主题、焦点、弹层和提示挂载位置。HeroUI 样式按实际渲染的组件逐个导入，新增组件时需同步补充样式入口。
- Shadow DOM 兼容包括样式变量映射、属性初始值及 React Aria 观察器生命周期补丁；内嵌界面保持浅色外观，在宿主边界明确文字颜色，避免独立弹层继承深色页面的白字。依赖升级时核对上游修复情况。
- 模型选择器使用分组、搜索及虚拟列表，手动输入保留自定义模型；列表更新通过 QueryClient 发布，避免直接修改共享查询对象。
- 子代理详情从原生会话控制器异步读取实际模型及思考深度；切换后丢弃旧响应，缺失时显示待获取，不使用父任务或角色默认值代替。
- 页面补丁按功能锚点和语义定位，匹配失败保留原代码及失败状态；同一渲染器资源的补丁结果按内容指纹缓存，重载不重跑。主进程脚本复用 Node 原生加载器读取的源码，在编译前修改，避免重复读盘并保留模块加载语义。主进程补丁优先走 `NODE_OPTIONS=--require`，Inspector 为回退（二者不同时使用）；CLI 包装器只覆盖 app-server 参数。Inspector / `--require` 专属修改不能因 CLI 启动成功就宣称生效。

## 会话、插件与提示词

- 会话导入、导出、删除轮次和恢复备份先释放目标会话，再校验大小、并发变化和路径；无法稳定确认目标时拒绝删除。永久删除在单次操作内复用扫描，并逐库检查记录路径。
- 会话索引及元数据读取已有 SQLite 和 rollout，发现缓存随文件替换、删除和 WAL 变化失效；启动期写入只在 Codex 停止后执行。
- 页面仍显示运行中时，完成态校准会复核原生任务状态并通过原生恢复流程重读；校准期间切换任务后补查当前任务。该流程不校验已经中断或空闲任务的历史完整性，索引时间也不能作为消息完整性的依据。
- 插件市场修复使用随包快照及可回滚替换，读取状态不触发修改；Computer Use 沿用官方统一插件和 `cua_repl`。主进程在生成插件配置时使用缓存的兼容副本，同步延长浏览器策略初始化和网络请求期限，保留身份及策略校验；原始安装文件不变，版本结构不匹配时使用官方入口并记录原因。
- 提示词优化使用运行中的路由或独立配置，后端校验地址、认证和模型。成功 HTTP 响应正文无效时直接报错，仅明确 404 保留兼容端点处理。

## 子代理与 FastCtx

- 增强在原生 macOS、Windows 上生成五种 Codey 角色和内部默认角色，复用 Codex agents 工具及生命周期 Hook；角色选择提示按用途推荐，不替代权限检查。
- Hook、角色副本和策略仅写入本次运行资产；Hook 状态锁与编排账本锁按会话目录隔离，跨会话只读取原子写入的账本快照。只读角色最多并行三个，出现写入角色时最多两个，由根代理在所有尝试结束后验收。
- 模型和思考深度按对应线路能力校验。内置目录回退时只能将唯一线路角色还原为原始模型；启动校验失败只关闭本次增强并保留设置，热更新失败保留原运行配置。
- 内置目录模式下，启动已启用增强则线路与模型映射在重启前固定；角色模型、深度和排序仍可在映射不变时热更新。
- 门禁结合任务绑定、能力证明和账本限制工具，策略缺失或身份不明时保守拒绝。仅完整无筛选列表可全量核对状态，等待超时不视为任务结束。
- 只读 SQL 使用保守词法检查，仍须数据库只读账号；Hook 和角色名不构成操作系统沙箱。跨 app-server 的写入互斥仍需调用方安排。
- 视觉角色获得对应图像及界面工具能力，协议回退保留图像块。任务载荷缺失或解密失败只触发一次任务重述，不尝试本地解密。
- 内置 FastCtx 提供文件读取、搜索、发现和批量替换；已有服务不重复注册，使用进程覆盖加载，不写入用户配置。

## 请求日志与额度估算

- 日志默认关闭，可热启停；主路径有界、非阻塞观测，独立线程写 SQLite，保留 NDJSON 调试输出。过载允许丢记录，不阻塞模型请求，不作为计费或审计账本。
- 日志记录线路、模型、状态、上游传输、阶段耗时、Token 和服务档位；正常提示词、响应与凭据不落盘。请求头记录最终构造结果，仅公开安全字段的值；HTTP 传输层自动生成的字段不在记录内，WebSocket 只在新建握手时记录。错误正文脱敏后最多保留 64 KiB，仍可能含供应商回显的请求片段。
- 列表按时间和请求 ID 游标分页，筛选范围固定；统计使用独立只读事务汇总和分桶，不受当前页影响。缺失用量保留未知，缓存及推理子项不重复加入总 Token。
- SQLite 使用单写入线程、批量事务和 WAL；批次有限重试并按请求 ID 去重，保留清理分批执行。健康状态暴露采样、待写入、写入错误和丢弃数，备份须取得一致副本。
- 官方额度优先读取账号用量，失败回退通用额度，不合并模型专属额度。成功缓存共享 60 秒，账号变化失效；有效周快照可在刷新失败时标记过期使用。
- 周限估算从日志入口读取官方账号当前周周期全部请求，以 API 等值费用除以官方已用比例推算总限额；当前速度的整周消耗单独计算。比例为零、周窗无效或缺失时不猜测结果。
- 费用按模型、实际档位和长短上下文独立计价，明确请求档位次之，未记录档位使用 Standard 并标注推定；未知价格不纳入金额合计。
- 缓存读写按输入子集计费，缺失写入量单独提示；GPT-6 Astra 缓存读取使用自定义双倍单价。工具、存储等不完整计量未计入，地区不另加价，估算不代表订阅实际扣费。
- 估算完整分页后才提交，关闭时停止后续读取；日志可能缺失或包含其他账号历史，官方额度可能包含其他设备用量，跨页读取也不是事务快照。

## 通知、诊断与更新

- 飞书、企业微信、Telegram 和微信 ClawBot 共用任务状态及去重机制，最多保存 32 个渠道；送达状态不明时不盲目重发，除超时与限流外的 4xx 视为配置错误直接结算，不再自动重试。
- 等待介入同时识别同步、异步提问；异步 accepted 只表示问题已发布，按实际回答清除等待，失败或终态也会释放状态。
- Trace 与 Crashpad 分别按设置执行保护和手动清理，复用互斥锁、文件白名单及静默期保护，不处理会话或账号数据；清理失败不将缺失统计显示为零。启动各阶段耗时（更新检查、登录探针、模型同步、运行时各步）以毫秒写入诊断日志。
- 更新检查读取发布清单，与官方账号登录探针并行进行，安装前走统一退出清理。构建将前端及注入脚本嵌入 Rust 程序，内容未变时保留产物时间戳以复用增量编译；平台打包及发布由现有脚本和工作流负责。
